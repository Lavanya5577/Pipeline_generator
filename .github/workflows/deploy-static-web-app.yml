name: Deploy Static Web Application

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build Static Web Application
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Prepare the static web application
      - name: Prepare Static Web Application
        run: |
          echo "Ensuring all HTML, CSS, and JavaScript files are correctly structured and optimized..."
          # Add any build or optimization commands here if necessary

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./ # Adjust this if your static files are in a subdirectory